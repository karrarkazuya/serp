<odoo>
  <data>
    <!-- explicit list view definition -->
    <record model="ir.ui.view" id="ssw_templates_inputs_list">
      <field name="name">tasks templates</field>
      <field name="model">ssw.proc.templates.inputs</field>
      <field name="type">list</field>
      <field name="arch" type="xml">
          <list>
            <field name="name"/>
          </list>
      </field>
    </record>

    <!-- explicit list view definition -->
    <record id="ssw_templates_inputs_form" model="ir.ui.view">
        <field name="name">ssw.proc.templates.inputs.form</field>
        <field name="model">ssw.proc.templates.inputs</field>
        <field name="arch" type="xml">
            <form string="Create tasks templates Form">
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="template_id" invisible="True"/>
                        <field name="computed_template_id" invisible="True"/>
                        <field name="computed_parent_template_id" invisible="True"/>
                        <field name="is_required"/>
                        <field name="type"/>
                        <field name="reference_model_name" invisible="type != 'model'"/>
                        <field name="allow_multiple_model_values" invisible="type != 'model'"/>
                        <field name="inputs_sub" invisible="not id or type != 'select'" domain="[('deleted', '=', False),('input_id', '=', id)]"/>
                    </group>

                    <notebook invisible="not id">
                      <page name='ss_workflow.show_in_other_tasks' string="Show in other tasks" autofocus="autofocus" sequence="0">
                        <field name="show_in_other_tasks" domain="[('template_id', '=', computed_parent_template_id), ('id', '!=', computed_template_id),('deleted', '=', False)]"/>
                      </page>
                    </notebook>
                </sheet>

                <chatter reload_on_post="True"/>
            </form>
        </field>
    </record>

    <!-- Search View -->
    <record id="view_ss_workflow_templates_inputs_search" model="ir.ui.view">
        <field name="name">ssw.proc.templates.inputs.search</field>
        <field name="model">ssw.proc.templates.inputs</field>
        <field name="arch" type="xml">
            <search string="Search Section">
                <filter 
                string="Active" 
                name="not_deleted"
                domain="[('deleted', '=', False)]"
                />
                <filter 
                string="Deleted" 
                name="deleted"
                domain="[('deleted', '=', True)]"
                />
            </search>
        </field>
    </record>


    <!-- actions opening views on models -->
    <record model="ir.actions.act_window" id="ss_workflow.action_templates_inputs">
        <field name="name">tasks templates</field>
        <field name="res_model">ssw.proc.templates.inputs</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id" ref="view_ss_workflow_templates_inputs_search"/>
        <field name="context">{'search_default_not_deleted': 1, 'search_default_deleted': 0}</field>
    </record>

  </data>
</odoo>
