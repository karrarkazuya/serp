<odoo>
    <!-- explicit list view definition -->
    <record model="ir.ui.view" id="jtemployees_extrashortages_list">
      <field name="name">Manual Shortages</field>
      <field name="model">jtemployees.extrashortages</field>
      <field name="type">list</field>
      <field name="arch" type="xml">
        <list>
            <field name="name"/>
            <field name="employee_ids"/>
            <field name="hours"/>
            <field name="date"/>
            <field name="company_id" readonly="1"/>
        </list>
      </field>
    </record>

    <!-- explicit list view definition -->
    <record id="jtemployees_extrashortages_form" model="ir.ui.view">
        <field name="name">jtemployees.extrashortages.form</field>
        <field name="model">jtemployees.extrashortages</field>
        <field name="arch" type="xml">
            <form string="Create Manual Shortages">
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="employee_ids"/>
                        <field name="hours"/>
                        <field name="date"/>
                    </group>
                </sheet>

                <chatter reload_on_post="True"/>
            </form>
        </field>
    </record>

    <!-- Search View -->
    <record id="view_jtemployees_extrashortages_search" model="ir.ui.view">
        <field name="name">jtemployees.extrashortages.search</field>
        <field name="model">jtemployees.extrashortages</field>
        <field name="arch" type="xml">
            <search string="Search Section">
                <filter 
                string="Active" 
                name="not_deleted"
                domain="[('deleted', '=', False)]"
                />
                <filter 
                string="Deleted" 
                name="deleted"
                domain="[('deleted', '=', True)]"
                />
            </search>
        </field>
    </record>


    <!-- actions opening views on models -->
    <record model="ir.actions.act_window" id="jtemployees.action_extra_shortages">
        <field name="name">Manual Shortages</field>
        <field name="res_model">jtemployees.extrashortages</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id" ref="view_jtemployees_extrashortages_search"/>
        <field name="context">{'search_default_not_deleted': 1, 'search_default_deleted': 0}</field>
        <field name="domain">[('company_id', 'in', allowed_company_ids)]</field>
    </record>

</odoo>
