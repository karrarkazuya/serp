<odoo>
  <data>
    <!-- explicit list view definition -->
    <record model="ir.ui.view" id="jtemployees_wall_list">
      <field name="name">Announcement Wall</field>
      <field name="model">jtemployees.wall</field>
      <field name="type">list</field>
      <field name="arch" type="xml">
        <list>
          <field name="name"/>
          <field name="details"/>
          <field name="expires"/>
          <field name="image"/>
        </list>
      </field>
    </record>

    <!-- explicit list view definition -->
    <record id="jtemployees_wall_form" model="ir.ui.view">
        <field name="name">jtemployees.wall.form</field>
        <field name="model">jtemployees.wall</field>
        <field name="arch" type="xml">
            <form string="Create Wall Form">
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="details"/>
                        <field name="departments"/>
                        <field name="expires"/>
                        <field name="image" widget="image" height="600"/>
                    </group>
                </sheet>

                <chatter reload_on_post="True"/>
            </form>
        </field>
    </record>



    <!-- Search View -->
    <record id="view_employees_wall_search" model="ir.ui.view">
        <field name="name">jtemployees.wall.search</field>
        <field name="model">jtemployees.wall</field>
        <field name="arch" type="xml">
            <search string="Search Section">
                <filter 
                string="Active" 
                name="not_deleted"
                domain="[('deleted', '=', False)]"
                />
                <filter 
                string="Deleted" 
                name="deleted"
                domain="[('deleted', '=', True)]"
                />
            </search>
        </field>
    </record>


    <!-- actions opening views on models -->
    <record model="ir.actions.act_window" id="jtemployees.action_wall">
        <field name="name">Announcement Wall</field>
        <field name="res_model">jtemployees.wall</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id" ref="view_employees_wall_search"/>
        <field name="context">{'search_default_not_deleted': 1, 'search_default_deleted': 0}</field>
    </record>

  </data>
</odoo>
