<odoo>

    <record id="view_employee_select_wizard_form" model="ir.ui.view">
        <field name="name">jtemployees.employee.select.wizard.form</field>
        <field name="model">jtemployees.employee.select.wizard</field>
        <field name="arch" type="xml">
            <form string="Select Employees">
                <group>
                    <field name="employee_ids"
                           widget="many2many_tags"
                           options="{'no_create': True}"/>
                </group>
                <footer>
                    <button string="Apply"
                            type="object"
                            name="action_apply"
                            class="btn-primary"/>
                    <button string="Cancel"
                            special="cancel"
                            class="btn-secondary"/>
                </footer>
            </form>
        </field>
    </record>

</odoo>
